version: '3.8'
services:
  node-app:
    build:
      context: .
      target: development
    container_name: mycont
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development

  mongodb:
    image: mongo:7
    container_name: mongodb
    ports:
      - "27017:27017"
